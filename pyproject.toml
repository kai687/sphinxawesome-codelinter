[tool.poetry]
name = "sphinxawesome-codelinter"
version = "0.1.0"
description = "A Sphinx extension that enables running linters over code blocks"
authors = ["Kai Welke <kai.welke@gmail.com>"]
packages = [
  # { include = "codelinter", from = "sphinxawesome" }
  { include = "sphinxawesome" }
]

[tool.poetry.dependencies]
python = "^3.5"
sphinx = "^2.2.2"

[tool.poetry.dev-dependencies]
pytest = "^4.6"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
